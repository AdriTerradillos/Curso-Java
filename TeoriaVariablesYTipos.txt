
/*
 ==== VARIABLE ====
 * En una variable podemos almacenar información.
 * Java es de tipado estático: cada variable tiene un tipo asignado.
 * El tipo que tiene asignado una variable es invariable, no puede cambiarse.
 * Cuidado si se hacen operaciones y es necesario almacenar el resultado en una nueva variable.
 * Hay que marca el tipo de esa variable que se vá al ser el resultado.
 *
 * ----JERARQUIA-----
 * doble flotar - largo, int corto = char "byte"
 *
 * --------
 * En operaciones aritméticas va a ganar siempre el tipo de dato mayor.
 * int . Long = largo
 * flotar * int = flotar
 * Es importante tener en cuenta esto cuando alojamos el resultado en una nueva variable.
 * int / int = int --áqué pasa si el resultado tiene que ser doble?
 * No es suficiente con que la variable del resultado se declare como doble.
 * La operación es entre dos enteros y el resultado tendrá la parte entera, perdiendo decimales.
 *
 * Para solucionar esto se necesita castear una de los números a un tipo decimal.
 * Como ganará el decimal el sí resultado tendrá la parte decimal.
 *
 * -----CASTING---
 * Para cambiar de un número de menor tamaño a otro de tamaño no hay problema.
 * Para hacer al revés, de un alcalde a otra menor que haga el casteo.
 * Es como forzar la conversión de un tipo de dato a otro.
 *
 * (float) int / int = float --- si esto se almacena en una variable doble
 * El resultado será flotante, pero al ser doble la variable
 * no hay problema y no es necesario el casteo.
 *
 * == ===== =====
 * = EJERCICIO =
 * == ===== =====
 * Se trata de que os familiaricéis con el uso de variables.
 * Vamos a hacer diferentes acciones con ellas.
 *
 * 1. Declarar variables de cada tipo sin valor.
 * 2. Asignarles un valor.
 * 3. Declarar nuevas variables asigns valores de las variables ya existentes.
 * 4. Declarar nuevas variables asignanoles un valor directamente.
 * 5. Practicar el uso de comentarios que indican en todo momento qué se está haciendo.
 * 6. Realiza operaciones entre variables numéricas
 * - Operador de suma
 * - Operador de resta -
 * - Operador de multiplicación *
 * - Operador de división /
 * - Operador de módulo % --o (opcional)
 * 7. Operaciones entre variables de diferentes tipos, cuidado con el tipo resultante.
 * 8. Operaciones entre números enteros (byte, corto, int, long) cuyo resultado sea decimal (flotar, doble)
 *
 * -----BONUS---
 * 9. Se puede operar con char? -Cómo?
 *
 *
 ====REALIZACION====
 * Para ver los resultados y comprobar que es lo correcto y esperado
 * System.out.println(); ----haz prints
 * Añade comentarios
 * // ---- comentario de una línea
 * * /* --o abre punto de referencia multilínea
 * */ // Aclaración comentario multilínea


// No te olvides de la clase...

    / Necesitas método main...

        // Variables de declaración: bytes, corto, int, largo, flotante, doble, char, boolean

        // Asigna valor a las variables

        // Vánicas NUEVAS, signos el valor de variables creadas anteriormente
        // Reasigna el tipo mediante casteo en de ellas, de ser necesario

        // Declara NUEVAS variables asigns valor, puedes hacer más de una de cada tipo

        // Experimenta con los operadores (a, -, *, /)
        / Recuerda/ que se necesita ajustes en los tipos a través de casteo
// Variables de Crea para los resultados, ejemplo: resultadoSuma


clase pública B-VariablesYTipos
el vacío estático público principal (String[] args)
boolean tieneO = false;
Cuadro de texto = "Hola";

        for (char l : texto.toUpperCase().toCharArray ())
si (l == 'o')
tieneO = true;
        -
        -

si (tieneO == verdad)
            System.out.println (""tener la letra 'O'");
                                        Si no,
                                System.out.println ("No contiene la 'O'");*/
     